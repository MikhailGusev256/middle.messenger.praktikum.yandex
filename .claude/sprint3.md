# Функциональные требования
## 1. Роутинг приложения
- Реализуйте роутер согласно имплементации из уроков.
- Настройте маршруты для всех страниц приложения:
  - / — страница входа
  - /sign-up — страница регистрации
  - /settings — настройки профиля пользователя
  - /messenger — чат
  - /404
  - /500
- Реализуйте переходы между страницами по клику. Например, кликнув по ссылке «Вернуться» на странице настроек, вы возвращаетесь к списку чатов.
- Обеспечьте корректную работу истории браузера: навигация по кнопкам «Назад» и «Вперёд» должна функционировать без сбоев.
- При перезагрузке (например, с помощью F5/⌘+R) пользователь должен остаться на той же странице.
- В DOM одновременно может быть активна только одна страница, поэтому при переходе на новую убедитесь, что предыдущая удалена.
## 2. Интеграция клиента с HTTP API
- Внедрите регистрацию, вход и выход из системы.
- Реализуйте управление профилем пользователя (изменение данных, аватара и пароля) и работу с чатами (получение списка, создание новых чатов, а также возможность добавить и удалить пользователя).
- После регистрации пользователь должен перенаправляться на страницу чата.
  Документацию по API вы найдёте [здесь](https://ya-praktikum.tech/api/v2/swagger/#/).                
  При работе с API используйте только тестовые данные: никаких реальных имён, email или телефонов. Добавьте простую проверку авторизации в роутер, чтобы перенаправить неавторизованных пользователей.
## 3. Класс для работы с запросами
- Используйте только Promise и XHR. Fetch, axios и аналогичные библиотеки запрещены.
- Реализуйте поддержку методов GET, POST, PUT и DELETE.
- Настройте обработку query string для GET и body для остальных методов.
# Нефункциональные требования
## 1. Система сборки
- Проект собирается с помощью Vite.
- Сборка завершается без ошибок.
- Автотесты запускаются и проходят успешно.
- Ветка спринта называется sprint_3.
## 2. Ограничения библиотек
- Разрешены только инструменты из предыдущих спринтов.
- Сторонние библиотеки запрещены, кроме препроцессоров, TypeScript и шаблонизатора.
## 3. Документация
- Обновите README.md с информацией о функциональности и инструментах.

# Чек-лист для самопроверки: спринт 3
## Роутинг
### Настройка и функциональность
- В проекте настроен роутинг.
- Страницы открываются по адресам:
  - /
  - /sign-up
  - /settings
  - /messenger
  - /404
  - /500
- История браузера подключена.
- Кнопки «Вперёд» и «Назад» работают.
- При перезагрузке (F5/⌘+R) пользователь остаётся на текущей странице.
- В DOM одновременно активна только одна страница.
### Переходы между страницами
- Переходы по кнопкам интерфейса выполняются корректно.
- Навигация через роутер и интерфейс браузера («Назад»/«Вперёд») работает.
- При переходе предыдущая страница удаляется из DOM.
## HTTP API и авторизация
### Авторизация
- Регистрация, авторизация и выход из системы работают корректно.
- После регистрации или входа пользователь попадает на страницу чата.
- Неавторизованные пользователи перенаправляются на страницу входа (у них нет доступа ни к какой из страниц, кроме / и /sign-up).
#### Работа с пользователем
- Данные пользователя, аватар и пароль можно изменять.
- Аватар загружается и отображается в профиле.
### Работа с чатами
- Список чатов пользователя загружается.
- Можно создать новый чат.
- Можно добавить пользователя в чат или удалить его из чата.


# Рекомендации для 3-го спринта
## Работа с API
### Обработка ошибок.
Обрабатывайте исключения специфично. Избегайте общих try...catch с console.log или простым Error. Для каждого типа ошибки API генерируйте максимально конкретные исключения.
### Безопасность данных.
Используйте только тестовые данные. Не указывайте реальные персональные данные.

## Архитектура роутинга
- Управление состоянием. При переходах между страницами состояние приложения должно корректно сохраняться и восстанавливаться. Настройте правила доступа: неавторизованным пользователям доступны только страницы / или /sign-up, а авторизованные необязательно видят страницу логина.
- Проверка авторизации. Добавьте middleware в роутер, чтобы автоматически перенаправлять неавторизованных пользователей на страницу входа.

---


## Возможно понадобится доделать, чтобы логин работал без ручных настроек браузера — Cross-site cookies
Проблема: фронт и API на разных доменах → кука `authCookie` третья-сторонняя. Chrome по умолчанию её режет: `signin` возвращает 200, но кука не сохраняется, и `GET /auth/user` падает `401 Cookie is not valid`. «Разрешить third-party cookies» в Chrome — костыль только на своей машине; у проверяющего/в CI логин снова упадёт. Решение — сделать запросы same-origin через прокси (нужны обе среды, они делают одно и то же):
- [x] `vite.config` — `server.proxy` (+ `preview.proxy` и `cookieDomainRewrite: ''` — API ставит куку с `Domain=ya-praktikum.tech`, без переписывания браузер на localhost её отбрасывает).
- [x] Netlify: rewrite через `[[redirects]]` НЕ работает — он передаёт `Set-Cookie: ...Domain=ya-praktikum.tech` как есть, браузер на `*.netlify.app` куку отбрасывает («Cookie is not valid» после логина). Решение — edge-функция `netlify/edge-functions/api-proxy.ts`: проксирует `/api/v2/*` и вырезает `Domain` из `Set-Cookie`. В `netlify.toml` остался только SPA-fallback `/*` → `/index.html` (иначе F5 на `/messenger` отдаёт 404 Netlify). **Проверить логин на задеплоенном сайте после пуша.**
- [x] `apiUrl` в `src/api/constants.ts` — относительный `/api/v2/` (слэш на конце обязателен: `HTTPTransport` конкатенирует `apiUrl + 'auth'`).

---

## Критичные проблемы по итогам аудита (2026-07-17)

- [ ] **/404 и /500 рендерятся пустыми** — шаблон `error.ts` ждёт `errorCode`/`errorMessage`, но `Router.use()` (`router.ts:41`) создаёт `Route` с props `{}`, а в `routeConfig.ts` props не заданы. Прокинуть props из конфига маршрута в `Route`.
- [ ] **LoginForm не показывает ошибки API** — в шаблоне `login-form.ts` нет `{{{ Error error=error }}}`, поэтому «неверный пароль» пользователь не видит.
- [ ] **RegisterForm: опечатка `{{{ Error error="error" }}}`** (`register-form.ts:64`) — передан литерал `"error"`, на странице всегда виден текст «error». Должно быть `error=error`.
- [ ] **EditProfileForm / EditPasswordForm не вызывают `this.props.onDone?.()` после успеха** — после сохранения пользователь остаётся в режиме редактирования (Profile передаёт `onDone=changeToViewMode`, но переопределённые `onValidSubmit` его не зовут).
- [ ] **Утечка подписок в `connect.ts`** — результат `store.subscribe()` (функция отписки) игнорируется; каждый переход между страницами оставляет подписку уничтоженного блока навсегда. Отписываться в `componentWillUnmount`.
- [ ] **`componentDidMount` срабатывает при каждом рендере** — `block.ts: render()` всегда зовёт `mountComponent()`. Следствие: `Chats.componentDidMount` дёргает `fetchChats()` при каждом `setProps` (например, при каждом выборе чата) — лишние сетевые запросы.
- [ ] **`logout` не чистит store** (`user-service.ts`) — `chats`, `selectedChatId`, `selectedChatUsers` остаются; следующий пользователь увидит чужие данные.
- [ ] **`chat-service.addUsers` не обновляет `selectedChatUsers`** — после добавления участника список «удалить пользователя» устаревший (а `removeUsers`, наоборот, не обновляет чаты).
- [ ] **Отладочные `console.log`** — `user-service.ts:22` (ответ регистрации), `http.ts:151`, `router.ts:84`; рекомендация спринта прямо запрещает console.log-подход.
